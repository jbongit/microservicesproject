server:
  port: 8083
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices_ratingdb
    username: root
    password: JB@12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  application:
    name: RATING-SERVICE
  config:
    import: configserver:http://localhost:8085

okta:
  oauth2:
    issuer: https://dev-72214406.okta.com/oauth2/default
    audience: api://default

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka